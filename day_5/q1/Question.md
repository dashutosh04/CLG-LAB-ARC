/*
Write a C program to create a TCP socket thorugh which two clients will connect to the server,
First client will send a number to the server, the server will forward that number to the second 
client will send that number to the server the server will forward that number to the first client.

(Swapping Server)
*/